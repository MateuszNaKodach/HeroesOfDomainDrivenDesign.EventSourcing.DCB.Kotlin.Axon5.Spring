spring:
  application:
    name: heroesofddd
  docker:
    compose:
      enabled: false
axon:
  axonserver:
    context: default
    enabled: true
logging:
  level:
    org.axonframework.configuration: DEBUG